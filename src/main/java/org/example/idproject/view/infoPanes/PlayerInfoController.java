package org.example.idproject.view.infoPanes;

import org.example.idproject.view.ScreenManager;
import org.example.idproject.viewmodel.DataProvider;

public class PlayerInfoController extends AbstractInfoController {
    public PlayerInfoController(DataProvider dataProvider, ScreenManager screenManager) {
        super(dataProvider, screenManager);
    }

    @Override
    public void update(int id) {

    }
}
